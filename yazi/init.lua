----------------------------
--          YAZI          --
----------------------------

require("full-border"):setup({
	type = ui.Border.ROUNDED,
})

require("zoxide"):setup({
	update_db = false,
})

require("git"):setup()
